services:
  app:
    build:
      context: .
    container_name: gov-clone-app
    ports:
      - "8080:80"
    volumes:
      - ./writable:/var/www/html/writable
    command: >
      sh -lc "chown -R www-data:www-data /var/www/html/writable && apache2-foreground"
    restart: unless-stopped
